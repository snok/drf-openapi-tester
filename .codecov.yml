# Docs: https://docs.codecov.io/docs/codecovyml-reference

codecov:
  require_ci_to_pass: true

coverage:
  precision: 1
  round: down
  status:
    project:
      default:
        target: auto
    patch: false
    changes: false

comment:
  layout: "diff,files"
  require_changes: true
